package com.Velouraz.velouraz.repository;

import com.Velouraz.velouraz.entity.PendantSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PendantSetRepository extends JpaRepository<PendantSet, Long> {
}